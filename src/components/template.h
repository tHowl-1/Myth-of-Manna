// COMPONENT TEMPLATE
/*
#pragma once

#include "../event.h"

using namespace mom;

namespace mom
{
	class CompC
	{
	public:
		bool null = false;

		type data;

		CompC() : null(true) {}
		CompC(type data) : data(data) {}

		void receiveEvent(Event* actionEvent)
		{
			switch (actionEvent->type)
			{
			case Event:
				break;
			default:
				break;
			}
		}
	};
}
*/